// if (nx == 1) set x = !x       // bitwise not


CHIP CHIPno {
    IN  
        in[16];  // 16-bit inputs        
    OUT 
        out[16]; // 16-bit output

    PARTS:
   		Not16(in=in, out=out);
	
}